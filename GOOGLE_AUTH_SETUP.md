# 🔐 Настройка Google Авторизации

## 📋 Что реализовано

✅ Google Sign In интеграция  
✅ Экран входа через Google  
✅ Отображение аватара и данных пользователя  
✅ Кнопка выхода из аккаунта  
✅ Сохранение сессии (auto sign-in)  

---

## 🚀 Для запуска в продакшене нужно:

### 1. Google Cloud Console Setup

1. Перейдите на https://console.cloud.google.com/
2. Создайте новый проект или выберите существующий
3. Включите **Google+ API**:
   - APIs & Services → Enable APIs and Services
   - Найдите "Google+ API" и включите

### 2. Создание OAuth 2.0 Client ID

#### Для Web приложения:

1. APIs & Services → Credentials → Create Credentials → OAuth 2.0 Client ID
2. Application type: **Web application**
3. Authorized JavaScript origins:
   ```
   https://your-domain.com
   http://localhost:5060
   ```
4. Authorized redirect URIs:
   ```
   https://your-domain.com
   http://localhost:5060
   ```
5. Скопируйте **Client ID**

#### Для Android приложения:

1. APIs & Services → Credentials → Create Credentials → OAuth 2.0 Client ID
2. Application type: **Android**
3. Package name: `eu.paintroom.paintroom`
4. SHA-1 certificate fingerprint (для debug):
   ```bash
   keytool -list -v -keystore ~/.android/debug.keystore -alias androiddebugkey -storepass android -keypass android
   ```
5. SHA-1 certificate fingerprint (для release):
   ```bash
   keytool -list -v -keystore /path/to/release-key.jks -alias your-alias
   ```

### 3. Обновление кода

#### Для Web:
Добавьте Client ID в `web/index.html`:
```html
<meta name="google-signin-client_id" content="YOUR_WEB_CLIENT_ID.apps.googleusercontent.com">
```

#### Для Android:
Ничего дополнительного не требуется - пакет `google_sign_in` автоматически использует SHA-1 fingerprint.

---

## 🧪 Текущая реализация (Demo Mode)

На данный момент приложение работает в **демо-режиме**:
- ✅ UI для входа через Google готов
- ✅ Логика авторизации реализована
- ⚠️ Для работы требуется настройка Google Cloud Console
- ⚠️ Без настройки кнопка входа не будет работать в продакшене

---

## 📱 Функции авторизации

### AuthService (`lib/services/auth_service.dart`)
- `signInWithGoogle()` - Вход через Google
- `signOut()` - Выход из аккаунта
- `currentUser` - Текущий пользователь
- `isLoggedIn` - Статус авторизации

### LoginScreen (`lib/screens/login_screen.dart`)
- Экран входа с кнопкой Google Sign In
- Отображение логотипа приложения
- Кнопка "Продолжить без входа"

### ProfileScreen (обновлен)
- Отображение Google аватара
- Отображение имени и email из Google
- Кнопка "Войти через Google" (если не авторизован)
- Кнопка "Выйти" (если авторизован)

---

## 🔧 Как протестировать

1. Откройте приложение
2. Перейдите на вкладку **Профиль**
3. Нажмите **"Войти через Google"**
4. На экране входа нажмите кнопку с логотипом Google

**Важно:** Без настройки Google Cloud Console авторизация не будет работать в продакшене.

---

## 📦 Зависимости

```yaml
dependencies:
  google_sign_in: 6.2.1
  shared_preferences: 2.5.3
```

Все зависимости уже добавлены в `pubspec.yaml`.

---

## 🌐 Поддержка платформ

- ✅ **Web** - Работает (требует Client ID в index.html)
- ✅ **Android** - Работает (требует SHA-1 fingerprint)
- ⚠️ **iOS** - Требует дополнительная настройка

---

## 🎨 UI Features

- Красивый экран входа с градиентом
- Логотип приложения (brush icon)
- Google кнопка с иконкой
- Индикатор загрузки при входе
- Обработка ошибок с уведомлениями

---

## 💡 Примечания

- Сессия сохраняется автоматически
- При перезагрузке приложения пользователь остается авторизованным
- Данные пользователя кешируются в SharedPreferences
- Выход из аккаунта очищает все данные

---

## 🚨 Требования для продакшена

1. ✅ Настроить Google Cloud Console
2. ✅ Получить Web Client ID
3. ✅ Получить Android SHA-1 fingerprint
4. ✅ Обновить `web/index.html` с реальным Client ID
5. ✅ Протестировать на реальном устройстве

---

**Текущий статус:** ✅ Реализовано, но требует настройки Google Cloud Console для работы в продакшене.
